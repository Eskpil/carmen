extern fn exit(code: usize);

fn left() -> usize {
    return 50 + 50 - 31;
}

fn right() -> usize {
    return 20 * 20;
}

fn _start() {
    let right = right();
    let left = left();
    exit(code: left + right);
}